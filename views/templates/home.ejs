<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
</head>
<body>
    <h1>HOME</h1>
    <hr>
    <h3>upload file</h3>
    <form action="/" method="POST" enctype="multipart/form-data"> <!--post on homepage-->
        <input type="file" name="thumbnail" accept="image/*">
        <input type="submit" value="submit" class="btn btn-primary">
    </form>
    <hr>
    <div class="image-container">
        <% if (thumbnailPath) { %>
            <img src="<%= thumbnailPath %>" alt="Thumbnail">
        <% } else { %>
            <p>No thumbnail available</p>
        <% } %>
    </div>
</body>

<style>
    .image-container {
    border: 1px solid black;
    width: 25vw; /* 25% of viewport width */
    height: 25vw; /* Set the height same as width to create a square container */
    overflow: hidden; /* Hide overflow content beyond the container */
}

.image-container img {
    width: 100%; /* Make the image fill the container */
    height: 100%; /* Make the image fill the container */
    object-fit: cover; /* Scale the image to cover the entire container */
}
</style>
</html>